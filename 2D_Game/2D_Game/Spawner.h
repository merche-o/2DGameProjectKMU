#pragma once

#include <list>
#include "AUnit.h"
#include "Item.h"

class Spawner
{
private:
	//std::list<AUnit*> & ennemyList;
	//std::list<Item*> & itemList;

public:
	Spawner(void);
	~Spawner(void);
};

